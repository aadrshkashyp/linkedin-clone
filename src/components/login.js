import styled from "styled-components";

const Login = (props) => {

    return (
        <Container>
            <Nav>
                <a href="/" alt="">
                    <img src="/image/login-login.svg" alt="" />
                </a>
            </Nav>
        </Container>
    )
};

const Container = styled.div``;

const Nav = styled.nav``;

export default Login;
